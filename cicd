@Library('lpl-library')_

def utils = new annotateEventToGrafana()


pipeline {
   agent any
    
stages {
        stage('Welcome Step') {
            steps { 
               scrip{
                utils.annotateEventToGrafana("deploy")
                 
               }
            }
        }
    }
}
