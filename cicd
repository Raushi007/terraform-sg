@Library('lpl-library')_

def utils = new annotateEventToGrafana()


pipeline {
   agent any
   environment {
        EXAMPLE_CREDS = credentials('grafana_token')
    }
    
stages {
        stage('Welcome Step') {
            steps { 
               script{
                utils.annotateEventToGrafana("deploy")
                 
               }
            }
        }
    }
}
